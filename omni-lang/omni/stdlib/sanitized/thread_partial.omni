module std::thread

// Minimal thread primitives for Stage0 bootstrap.

struct Mutex<T>:
    // stub

fn mutex_new<T>() -> Mutex<T>:
    return Mutex {}

struct CondVar:
    // stub

fn condvar_new() -> CondVar:
    return CondVar {}

struct AtomicBool:
    // stub

fn atomic_bool_new(v: bool) -> AtomicBool:
    return AtomicBool {}
