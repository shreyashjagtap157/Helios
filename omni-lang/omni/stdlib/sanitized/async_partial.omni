module std::async

// Preservation header: full original moved to
// omni/stdlib/preserved_full/async.omni
// Minimal async primitives for Stage0 bootstrap.

enum Poll:
    Ready
    Pending

trait Future:
    fn poll(&mut self) -> Poll:
        return Poll::Pending

fn ready() -> Poll:
    return Poll::Ready
