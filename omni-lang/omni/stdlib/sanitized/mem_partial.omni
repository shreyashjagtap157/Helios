module std::mem

// Minimal memory helpers for Stage0 bootstrap.

fn size_of<T>() -> usize:
    return 0

fn alloc(size: usize) -> u64:
    return 0

fn dealloc(ptr: u64, size: usize) -> ():
    return

struct Box<T>:
    ptr: u64

fn box_new<T>(val: T) -> Box<T>:
    return Box { ptr: 0 }
