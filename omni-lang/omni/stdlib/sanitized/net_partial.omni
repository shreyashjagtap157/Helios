module std::net

// Minimal networking stubs for Stage0 bootstrap.

struct Socket:
    // stub

fn connect(addr: str, port: u16) -> Socket:
    return Socket {}

fn send(s: &Socket, data: &[u8]) -> usize:
    return 0

fn recv(s: &Socket, buf: &mut [u8]) -> usize:
    return 0
