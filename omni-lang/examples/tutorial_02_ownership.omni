// Tutorial 02: Ownership and Borrowing
// This demonstrates Omni's ownership and borrowing features

fn main():
    // Ownership example
    let name = "Alice"
    let greeting = concat("Hello, ", name)
    println(greeting)
    println("Strings are moved by default")

fn concat(a, b) -> string:
    return a + b
